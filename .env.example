# 🔧 Configuración de Variables de Entorno para LOCAL- CartoLMM
# Copia este archivo como .env y ajusta los valores según tu entorno

# 🌐 Puerto del Servidor Web
PORT=8080

# 🏠 Host del Servidor
HOST=localhost

# 🚀 Modo de Ejecución
NODE_ENV=development

# 🔗 URL del backend blockchain (magnumslocal / magnumsmaster)
# En local, magnumslocal suele exponer HTTP en 6001 (ver magnumslocal/.env.local).
# Ajusta este valor si tu backend usa otro puerto.
BLOCKCHAIN_API_URL=http://localhost:6001

# (Opcional) Si en el futuro el frontend se migra a Vite/import.meta.env,
# mantener también la variante con prefijo VITE_.
VITE_BLOCKCHAIN_API_URL=http://localhost:6001

# 🕐 Timeout de Peticiones API (ms)
API_TIMEOUT=30000

# 🔐 CORS Origin (Socket.IO de CartoLMM)
# Debe ser el ORIGIN desde el que se abre el dashboard de CartoLMM.
# - Si ejecutas CartoLMM con `node server.js`: normalmente http://localhost:8080
# - Si ejecutas Vite dev (`npm run vite:dev`): normalmente http://localhost:5173
CORS_ORIGIN=http://localhost:8080

# 📊 WebSocket Configuration (si lo usas en scripts/clients)
SOCKET_CORS_ORIGIN=http://localhost:8080

# Ejemplo para producción:
# NODE_ENV=production
# BLOCKCHAIN_API_URL=https://api.tudominio.com
# CORS_ORIGIN=https://tudominio.com
# SOCKET_CORS_ORIGIN=https://tudominio.com
